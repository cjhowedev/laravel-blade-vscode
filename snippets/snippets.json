{
  "for": {
    "prefix": "@for",
    "body": [
      "@for ($1 = $2; $1 < $3; $1++)",
      "    $4",
      "@endfor"
    ],
    "description": "for"
  },
  "foreach": {
    "prefix": "@foreach",
    "body": [
      "@foreach ($1 as $2)",
      "    $3",
      "@endforeach"
    ],
    "description": "foreach"
  },
  "if": {
    "prefix": "@if",
    "body": [
      "@if ($1)",
      "    $2",
      "@endif"
    ],
    "description": "if"
  }
}
